# BMS — Broker Management System (Demo)

Mock Broker Management System for demonstrating personal lines automobile insurance quoting via an API gateway.

## How to Run

```bash
npm install
npm run dev
```

This starts the React frontend on `http://localhost:3000` and the mock API server on `http://localhost:4000`.
