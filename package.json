{
  "name": "bms-frontend",
  "version": "1.0.0",
  "description": "BMS - Broker Management System frontend for insurance quoting",
  "main": "index.js",
  "proxy": "http://localhost:4000",
  "scripts": {
    "start": "react-scripts start",
    "server": "cd server && node index.js || (echo. && echo === SERVER CRASHED === && pause)",
    "dev": "concurrently \"npm start\" \"npm run server\"",
    "build": "react-scripts build",
    "deploy": "gh-pages -d build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "homepage": ".",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gh-pages": "^6.3.0",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "react-scripts": "^5.0.1"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
